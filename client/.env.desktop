# Desktop (Electron) build. The window loads the app from the bundled backend
# at http://localhost:4000, so the client must talk to its own origin rather
# than the LAN IP in .env. A blank value makes api.ts use window.location.origin.
VITE_API_URL=
